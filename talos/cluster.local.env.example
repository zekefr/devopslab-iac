#!/usr/bin/env bash

# Optional local overrides for Talos bootstrap script.
# Base cluster data (nodes, IPs, gateway, DNS, endpoint) is generated from Terraform
# into talos/cluster.generated.env via:
#   make talos-sync

# Optional bootstrap node override (defaults to first control plane from generated config).
# BOOTSTRAP_NODE="cpk8s01"

# Optional hostname prefix override (defaults to "talos").
# NODE_HOSTNAME_PREFIX="talos"
